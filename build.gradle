plugins {
    id 'maven-publish'
    id 'org.jetbrains.kotlin.jvm' version '1.3.72'
}

allprojects {
    apply plugin: 'maven-publish'
    apply plugin: 'org.jetbrains.kotlin.jvm'

    repositories {
        mavenCentral()
    }

    dependencies {
        compileOnly "org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.3.72"
    }
}

group 'dev.jaims'
version '2.0.1'

subprojects {
    group 'dev.jaims.mcutils'
    version '2.0.1'

    publishing {
        publishing {
            // Publish to Github Packages
            repositories {
                maven {
                    name = "GitHubPackages"
                    url = uri("https://maven.pkg.github.com/jaimss/mcutils")
                    credentials {
                        username = project.findProperty("github.username") ?: System.getenv("github.username")
                        password = project.findProperty("github.token") ?: System.getenv("github.token")
                    }
                }
            }
            publications {
                gpr(MavenPublication) {
                    from components.java
                }
            }
        }
    }
}

compileKotlin {
    kotlinOptions.jvmTarget = "1.8"
}
compileTestKotlin {
    kotlinOptions.jvmTarget = "1.8"
}